# Template only — the value below is the base64 encoding of the literal
# placeholder string "REPLACE_ME", not a real credential. Generate the real
# secret out of band (e.g. `echo -n "<token from @BotFather>" | base64`)
# and apply it separately; never commit a real bot token here.
apiVersion: v1
kind: Secret
metadata:
  name: telegram-bot-secret
type: Opaque
data:
  bot-token: UkVQTEFDRV9NRQ==
